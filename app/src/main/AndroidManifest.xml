<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.misaya.imool" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.bluetooth_privileged"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.TeacherActivity" ></activity>
        <activity android:name=".Activity.SolveActivity"></activity>
        <activity android:name=".Activity.StudentActivity"></activity>
        <activity android:name=".Activity.RegistStuActivity"></activity>
        <activity android:name=".Activity.RegistTeaActivity"></activity>
        <activity android:name=".Activity.GuideActivity"></activity>
        <activity android:name=".Activity.ClassActivity"></activity>
        <activity android:name=".Activity.MineActivity"></activity>
        <activity android:name=".Activity.DiscoveryActivity"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>
